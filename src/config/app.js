// Demo mode switch (default true).
// true: use contract sendPlainEmail() plaintext demo path
// false: use production FHEVM externalEuint32 + attestation (requires @fhevm/sdk)
export const IS_DEMO = true;
